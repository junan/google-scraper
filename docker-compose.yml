version: '3.8'

services:
  app:
    build: .
    ports:
      - 8080:8080
    environment:
      - BEEGO_ENV=prod
      - DB_USERNAME
      - DB_PASSWORD
      - DB_HOST
      - DB_PORT=5432
      - DB_DATABASE
